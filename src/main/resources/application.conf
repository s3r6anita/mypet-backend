ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.example.ApplicationKt.module ]
    }
    security {
        jwt {
            secret = "nb1k?s7bn2m3L0!2"
            issuer = "my-pet"
            audience = "my-pet"
            claim = "email"
            realm = "realm"
            expiration_time = 10000
        }
    }
    storage {
        driverClassName = "org.postgresql.Driver"
        jdbcURL = "jdbc:postgresql://localhost:5432/mypet"
        user = "postgres"
        password = "1qazxsw2"
    }
}